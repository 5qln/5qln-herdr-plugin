# ATTESTATION — the bridge · the live desk adapter + the runtime config-read

*Amihai's act. One sentence in his own words, and the sha256 of what he attested. The machine wrote the
material below the line so he knows exactly what he is putting his word to; it never types his word for him.*

---

## What he ran, by his own hand

```
bash /home/deploy/the-cell/rounds/bridge/verify-live.sh
```

Its output, from his own box (python 3.12.3; the machine ran the identical script first — this is what it showed):

```
Python 3.12.3
Ran 44 tests in 25.061s
OK
6989a742f57ec60a54d44062bad3fe9c6d2df28e66e92de96c1336be3a6539c3  /home/deploy/the-cell/state/gates.jsonl
live turn process: None | socket: /home/deploy/.config/herdr/herdr.sock
```

## What is being attested

That **the bridge — the live desk adapter + the runtime config-read — does what it was commissioned to
do**, on the evidence of an execution record written by the non-author (`evidence.md`, **18/18 PASS —
C1–C7, K1–K5, six lenses — 6.37 s, zero corrections**), and an audit pack **accepted 43/43 against the
real interface before it judged anything** (the conforming run passes; a twin with five surgical defects —
C1 live-spawn · C4 boots-on-inconclusive · C5 socket surface · C6 drifted charge · K3 attested write —
fails exactly those axes plus the shared K1, and each is named, non-cascading).

- the **live desk mode (C1)** — `cost.DeskAdapter`'s third mode joins a turn to the live herdr socket
  through the imported B2 instrument (resolve by pane label → `agent.prompt` → fenced read), process
  `None`, no fixture spawn;
- **live mode fails closed (C2)** — an unreachable socket holds outage, a no-agent desk holds blocked
  `agent_not_found`, never a fake answer, never clean; the centre guard refuses S/podium before any byte;
- the **runtime config-read (C3)** — each desk's codex §2 emphasis, voice, model, and the budget are read
  from a soft-layer file at runtime, replacing the hard-coded `DESK_FUNCTION_SPECS`/`COST_MODEL` literals;
- **declared defaults + fail-closed (C4)** — absent config → B4's exact bytes; empty/malformed/partial →
  INCONCLUSIVE with the reason, the run refuses to boot, never a silently substituted value;
- **import, never re-author (C5)** — the dialect, D.12 checks, grammar and descent are imported sha-pinned;
  no new decoding operation, no new L1 symbol, no sixth corruption code;
- **nothing attested is un-done (C6)** — the two fixture modes still reproduce B4's declared bytes; only
  `live` was added; the default mode stays `re-prompted`;
- **cold restart (C7)** — a second fresh process re-arms the live mode + config-read from the ledger and
  soft files alone, byte-identical;
- stdlib-only/deterministic (K1) · byte-exact enumerated forms, never normalised (K2) · the click stays the
  human's, no machine `attested` write (K3) · the B2 centre guard holds (K4) · the soft config is a
  diff-able JSON data file (K5);
- the six lenses, including cold-restart and absence-never-valid.

**What is NOT being attested:** that a real desk beyond G has booted or answered a prompt — only G is
constituted on the box, so the live mode is tested against a fixture live server speaking the real
dialect plus a read-only live-socket probe (H-BRIDGE-1); a real `agent.prompt` to a constituted desk is a
paid Pi turn, deferred to the constitution; the soft-config location + schema are provisional (the
constitution writes the real soft files — H-BRIDGE-2); and nothing about the constitution, the settings
surface, the two modes, or the swarm — those are soft mode, not this firmware round.

## The drafted sentence — his to change, delete, or replace

> *"The conductor is joined to the live herdr socket through the attested instrument, and reads each
> desk's emphasis, voice, model and budget from the soft layer at runtime — live mode fails closed, the
> config-read falls back to the declared defaults, and nothing attested is un-done."*

**He may say it differently, or say only "attest it". The machine never types his word for him.**

## His word — recorded exactly as he gave it

> **"attest it"** — Amihai Loven, 2026-08-30. (He endorsed the drafted sentence above as written.)

---

- `evidence.md` sha256: `42a6d29a14d076e80992c87147703b05e7a991efc808800306adc9a337887218`
- `commission.md` sha256: `844705640475f7912f89406483e3f6031dffbc43a6587a242a7342045095de63`
- authored files (sha256): `run.py 4d550f88…` · `cost.py 58ed8ccd…` · `softconfig.py 17ccf2ba…` · `surface_contract.py 477ba561…` · `selftest.py a6f783cc…` · `phase-card.md 83ad58d0…`
- his box at the moment of attesting: `state/gates.jsonl` — 1 record, his plant (`6989a742…`; the bridge is firmware, not a gate)
- date: **2026-08-30**
