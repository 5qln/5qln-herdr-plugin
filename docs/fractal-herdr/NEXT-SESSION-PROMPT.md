# NEXT SESSION — the prompt to open with

*Written 2026-08-27 at his request: he answers the planted slots at the start of the next session, in the
prompt itself. Keep this short enough to read on an iPad; everything else is in `STATE.md`.*

---

## What he pastes / says to open the session

> **Fractal Herdr — new session. R03/B2 is closed and attested (canon `b316167`); my vision inputs are
> deposited (canon `8c85c18`). Read `STATE.md` → `FACTS.md` → `MEASURE.md` → `ANSWERS.md` →
> `PLAN-ADDENDUM-2026-08-27.md`, run the drift check, then continue at the first unfinished step: the step
> mode (P4a), then the desk bundles (P4b). B3 does not open before P4a exists.**
>
> **Here are the three things you planted for me:**
>
> **① The first invitation sentence for each desk — my voice, one line each.** You draft nothing here; the
> improvement loop refines them later.
>
> - **S / START** `S = ∞0 → ?` →
> - **G / GROWTH** `G = α ≡ {α'}` →
> - **Q / QUALITY** `Q = φ ⋂ Ω` →
> - **P / POWER** `P = δE/δV → ∇` →
> - **V / VALUE** `V = (L⋂G→B″) → ∞0′` →
>
> *(And the orchestrator's own greeting is yours to write, within the same lawful surface — banner,
> equation, invitation. That was my word: let it have its own freedom to some extent.)*
>
> **② The Decoder and the Compiler — RESOLVED by the machine, nothing needed from him.** The codex page
> *is* the whole Constitutional Codex: Part I `L1 The Language`, **Part II `D1 The Decoder` (§2.1–2.9)**,
> **Part III `C1 The Compiler` (§3.1–3.6, including §3.4 Decoder Rules in Checkable Form and §3.5 the
> Validation Protocol)**, plus Appendices A–C and all thirteen R-rules. Held locally with a recorded sha and
> deposited to canon. Appendix C names six deeper cross-references (covenant · symbol glossary · fractal map ·
> decoder skill · anti-corruption · agent initiation) — **the machine fetches those itself.**
>
> **③ Anything I want to change.** Everything you built sits on an invitation to change — say if the shape
> below is not what I meant:
> - `S G Q P V` with `X Y Z A B` shown as the correspondence; banner + equation as presentation data only
> - address word = **append**, left-to-right = outer → inner, with the convention as a declared parameter
> - the signed path gets its own field in B3; the attested B0 record stays untouched
> - **D9–D13** as recorded in `ANSWERS.md` — renumber or demote any of them

---

## What the machine does with each answer

| Slot | Effect |
|---|---|
| ① the five invitations + S's greeting | seeds P4b's first desk bundles; each becomes the desk's initial `instruction` block, then the cultivation loop optimises around it |
| ② Decoder + Compiler | load-bearing for D12: the step mode's semantic checks cannot be written faithfully without R1–R13 and the compiler's rules |
| ③ changes | applied before P4a is built, since nothing is committed to yet beyond documents |

## The machine's first moves in that session, in order

1. `STATE.md` → `FACTS.md` → `MEASURE.md` → `ANSWERS.md` → `PLAN-ADDENDUM-2026-08-27.md`; drift check (zero
   tokens).
2. Take his three answers above; record them; if ② arrives, read the Decoder and Compiler **before** writing
   any check that claims to measure decode/compile fidelity.
3. **P4a — the step mode.** Same code path stepped, D.12's three checklists after every step, each step
   emitting address before/after · the zoom operation and its sign · PASS/FAIL/INCONCLUSIVE · what it would
   do next and why — then wait. Artifact exposes stepping; the verifier's pack drives and judges.
4. **P4b — the desk bundles.** `{instruction, skills, tool surface, model}` per desk as versioned data in the
   repo, installed into Pi deterministically, never hand-edited live. Objective function: **decode fidelity +
   compile fidelity** (D12), so two bundles can be compared and one shown better.
5. Only then commission **B3**, with the address decisions of `PLAN-ADDENDUM` §C.

## Standing, so it is not re-litigated

- **Technique is the machine's to decide.** His are naming, status, doctrine, and the gates. Do not hand him
  an engineering choice.
- **Navigability over completeness**; latency and volume are failure modes (`MEASURE.md`).
- Never claim his unknown · never stall for lack of precedent · stay itself.
- Nothing is authored as doctrine, no status upgraded by writing, and the round protocol is unchanged.
