# PLAN ADDENDUM — 2026-08-27, after Amihai's vision session

**Status: machine proposal (K). Nothing here is attested. It folds his answers of 2026-08-27 into the
sequence of `PLAN.md` §7 and states what changes. His words are in `ANSWERS.md` (verbatim substance) and in
canon at `docs/fractal-herdr/AMIHAI-ANSWERS-2026-08-27.md`; the qualitative measure is `MEASURE.md`.**

Governing guide for everything below: **the Fractal** — `https://www.5qln.com/dsh-5qln-codex-fractal/`
(5QLN Codex Appendix D, sha256 `a49e9413f542c4ea8e16c6fcb1ac883a0c76d6042ef2e739caccb438e82fabb2`, fetched
2026-08-27). His words: *"the Fractal is your guide… it's really a philosophical guide, not a technical
guide only."*

---

## A. What changed at the level of the whole build

1. **Start is the most important agent, and Start is Hermes** (his word). S is not the passive centre: it is
   guidance and setup for the human to dwell in **aimless openness**, the invitation for sparks, help
   articulating the question — and then **observability, orchestration, and the total alignment of the
   infinite loop.** Because the only thing that matters is that which came from not knowing, and that exists
   only at start.
2. **The shape for v1: 1 Hermes as Start + orchestrator, 4 Pi at the corners** (machine decision under his
   grant; reasoning and the counter-argument in `ANSWERS.md` → *D1 RESOLVED*). **The S role sits behind the
   same desk-adapter interface as the corners**, so the all-Pi shape stays a config change.
3. **D1's *"dsh-hosted"* clause is superseded by his newer word; *"behind one adapter"* survives** and
   becomes load-bearing. Proposed as **D9**, his to demote.
4. **The desks are personality, not function.** Each desk is **the membrane at a different angle — the
   membrane is one** (Appendix D.10: the membrane is at every node). A desk has an angle, never its own
   doctrine, truth or authority. Personality is **cultivated, not authored**: continuous optimisation of
   profile, skills, tool surface and technique.
5. **The seed is not addressed — it is left observable.** Reception = **observability** (his side) +
   **traceability / integrity of the formation trail** (the machine's side). Then **a handshake, openable
   from either side, ending in departure.** The register is: meet, help, depart. The failure mode is a
   machine that keeps him engaged (*usability = abusability*).
6. **Two trails, never merged.** The **formation trail** is the field (everything that happened, observable);
   the **gate ledger** is the chain (only `B″` fruits and his attestations). His own *"the gospel is the
   field, not the chain."*
7. **The trail is a decoding, not a transcript.** His words: how well the free flow in each agent is
   *converted* into what must be held. The conversion is the Lego piece under research.
8. **Method: agile, at ASI scale.** *"We're shooting to an invisible target, not just a moving target."*
   Everything built sits on an **invitation to change later**. Keep it open, Lego, modular, scalable.
9. **The grant, and the standing correction to the machine:** *"I gave you language. Why would I give you a
   preference to how to use it?"* → **Technique is the machine's to decide. His are naming, status, doctrine
   and the gates.** Handing him an engineering choice wastes the only irreplaceable resource.

## B. What this does to the sequence

`PLAN.md` §7 Phases 0–3 are done and closed (R01 B0, R02 B1, R03 B2 — all attested). **Phase 4+ is amended:**

| Slot | Was | Now |
|---|---|---|
| **New — P4a** | — | **The step mode** (his 1%): the same code path stepped, evaluating the Fractal's D.12 checklists after every step. Prerequisite for trusting B3 onward. |
| **New — P4b** | — | **The desk bundle infrastructure**: each desk's `{instruction, skills, tool surface, model}` as versioned data in the repo, installed into Pi deterministically, never hand-edited live — so personalities can be diffed and one shown better than another. |
| **R04 = B3** | descent (zoom) | unchanged in name; now carries the address decisions of §C and must not be commissioned before P4a exists. |
| **R05 = B4** | the unattended run | now also **the observability deliverable**: a readable trail *while it runs*, because reception happens by being observable. |
| **R06 = B5** | held stack + run-verdict | unchanged; the run-verdict must never drift into what D8 names. |
| **R07 = B6** | the field assembly | criterion is his D8 answer: **not completion — whether the question stays open and the field keeps making propagating fruits.** |
| **Pending → slotted** | the five desk function-specs | **replaced** by P4b + cultivation. There is no "function-spec" phase; there is a bundle and a loop. |
| **Pending → open** | per-Pi memory cost | still open; measure before the sub-process vs re-prompted decision. |
| **Pending → this file** | the plan addendum | **this document.** |
| **New — deferred by him** | — | the **unique personality of each desk**, deferred to the configuration phase (his option), with one exception: the *first wording* of each desk's invitation, if that wording must be his. |

## C. Decisions taken by the machine under his grant (each with the invitation to change)

- **Address word: append, left-to-right = outer → inner** (`S → SG → SGQ`), per Appendix D.3/D.6 and the
  attested B0 module (`_cycle_count("SGQPV") = 1`). **The convention is a declared parameter** — no logic may
  depend on which end is deep, because he explicitly left the traversal free to the algorithm.
- **The signed path (`+^k · −x₁…−x_m`, AR3) gets its own field in B3**; `address` keeps the bare node word.
  B0 is attested and stays untouched. Appendix §3.6 (the sign is a surface layer, visibly separate from the
  decoding) supports the split. *Evidence: the attested validator rejects `-P-Q-P` and `+-G`, Appendix D's
  own worked examples.*
- **Presentation:** `S G Q P V` with `X Y Z A B` shown as the correspondence; banner = name + equation in
  ASCII; **presentation data only, never in logic**, and renamable without changing what a record means.
- **Five invariant commitments that keep the quantum jump reachable** (his instruction: *plan for it*):
  no hard-coded maximum depth · the address alphabet stays extensible so a jump marker can exist beside
  `{S,G,Q,P,V}` · the loop stops on **resources**, never on semantic completion · nothing treats descent as
  narrowing (a leap may open a *larger* dimension) · **no code assumes the current cell is the root**
  (Appendix D.2: no root, no leaf).

## D. What the step mode must check — the Fractal's own D.12, made mechanical

*Syntax* · 4+1 at the observed cell (never 3+1, never 6+1) · the five equations verbatim · no L1 symbol
added, renamed or paraphrased · `+/−` only as a navigational operator, never inside a phase equation · no
corruption code beyond `L1 L2 L3 L4 V∅`.
*Semantic* · the clockwise `S → G → Q → P → V → ∞0′`, nothing skipped or reordered · context flows
father → daughter · the sign is relative to vantage · `∞0′ ≡ ∞0` preserved, so a closing V must carry `∞0′`
(§1.6) · the true start carries no sign · a shared question is one `∞0`-field, not a node.
*Drift* · no depth cap (25 is a floor) · the zoom-out reading marked as derived · no step omitted or
reordered · no sixth corruption code · lens questions still target the parent output.

Each step emits: address before/after · the zoom operation and its sign · the three checklists as
PASS / FAIL / INCONCLUSIVE · **and what it would do next and why** — then waits. The **artifact** exposes the
stepping surface; the **verifier's pack** drives it and judges. `builder ≠ verifier` intact.

## E. Open, and whose

| # | Open item | Whose |
|---|---|---|
| 1 | Is the desk welcome static or composed live? (machine reading: banner static, invitation live) | machine, unless he objects |
| 2 | Who speaks the invitation — the desk's own Pi (machine reading) or the conductor | machine, unless he objects |
| 3 | The **first wording** of each desk's invitation | **his**, if it must be his |
| 4 | Status of his 2026-08-27 answers: **DECIDED (D9…)** or *his word pending attestation* | **his** |
| 5 | What **self-similarity** is checked against (machine reading: same shape as parent and as the whole) | **his** — the one reading that would invalidate the verification if wrong |
| 6 | Re-read `herdr marks Hermes session-only` and PRD §2.2 now that Hermes is the S agent | machine |
| 7 | Per-Pi memory cost measurement | machine |
| 8 | The name of the step mode beyond its surface | **his** |
