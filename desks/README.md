# desks/ — the five desk constitutions (the soft layer)

*CANDIDATE — seeded from Amihai's sealed words ("perfect, use it"); the cultivation loop refines them.
Never authored once, never "done" by being written. The live box beats this copy for state; the repo
beats the box for revision.*

The soft layer of the cell: the five seats (`S · G · Q · P · V`) as plain files that **activate the
firmware, never modify it**. This is W3's deliverable — the constitution, not the engine.

## One desk = the seat + boot + tool surface + conduction guide

| File | What it is |
|---|---|
| `SYSTEM.md` | the seat — first-person "I am…" + the sealed codex + the §3.6 surface (`⟦SURFACE v1⟧ … ⟦END SURFACE⟧`) + the `⟦END …⟧` answer fence |
| `AGENTS.md` | boot + tool surface (the pi reads this at launch) |
| `boot.sh` | the launcher — model + provider, one seat per pane |
| `.pi/settings.json` | model · provider · thinking level · tools (`deepseek-v4-pro`, `read`/`grep`/`bash`) |
| `.pi/prompts/guide.md` | the conduction guide — re-pointed in R08 to call the seam, never hand-drive desks |

`S` alone carries `.pi/extensions/pi-cell/` — the thirteen `cellctl` slash tools (`/conduct` and the
rest). Its authored source canon lives at `rounds/R08-bindings/authored/pi-cell/`; this copy is the
deployed instance.

## The correspondence (what a desk embodies vs activates)

- **embodies — ASIC (held, untouched):** the codex seal (`H = ∞0 | A = K`, the five equations, `No V
  without ∞0'`, the L-codes) and the §2 decoding operations in attention mode.
- **activates — firmware (built + attested):** the Grammar decoder/compiler in step mode · the bridge's
  live desk mode · orchestration `word.py`/`navigate.py` · the `softconfig` runtime read.
- **rewrites — soft layer (this dir):** emphasis/voice/model/budget + skills + recipes — *inside the
  enforcement wall* (L1 no driving logic · L2 the seam is the only path · L3 config must validate).

## The one rule

The podium is human-planted only. No desk writes the question; no desk attests; no desk claims ∞0.
`S` is the conductor — it calls `/conduct`, and the engine drives G·Q·P·V through the one wire.
Conduction is one line: **conduction = call `/conduct`**.
