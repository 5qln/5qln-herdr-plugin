# ATTESTATION — R04 · B3 · the descent (zoom)

*Amihai's act. One sentence in his own words, and the sha256 of what he attested. The machine wrote the
material below the line so he knows exactly what he is putting his word to; it never types his word for him.*

---

## What he ran, by his own hand

```
/home/deploy/the-cell/rounds/R04-B3/verify-live.sh
```

Its output, from his own box (python 3.12.3; the machine ran the identical script first — this is what it showed):

```
all 59 checks predicted — the verifier writes the only record that counts
word order (declared parameter): inner_first — D.2 inner-first carried; D.3/D.6 outer-first stays HIS to confirm (H-B3-2)
the letter-order touches exactly two spots: zoom_in delegates to grammar.seat_address; zoom_out is the only WORD_ORDER branch
sealed corruption codes (no sixth): L1 L2 L3 L4 V∅
```

## What is being attested

That **B3 — the descent (zoom) — does what it was commissioned to do**, on the evidence of an execution
record written by the non-author (`evidence.md`, 18/18 PASS, zero corrections, 0.79 s):

- byte-exact axis inheritance — a 3-deep descent shows byte-identical `axis.field` from root to leaf, and a manufactured field change yields MOVING and a stop-and-surface (C1);
- address append/strip + the signed-path split — addressing is derived, never stored; the signed path (`+^k · −x₁…−x_m`, AR3) is a separate field; `-P-Q-P` and `+-G` are rejected (C2);
- the guard pass at every depth (L1 L2 L3 L4 V∅) — a V with no ∞0′ is refused (C3);
- the return criterion = artifact + genuine ∞0′ — an unattested V is held, never judged genuine (C4);
- TENTATIVE is temporal, never epistemic — a consumed tentative seed is refused, no heuristic promotes it, and there is no podium write path (C5);
- the gate-fails-to-lock flow → child node + address append + a seated arrangement (C6);
- the five quantum-jump commitments, as structural constraints never a feature (C7);
- stdlib-only, deterministic, no LLM (K1) · no byte normalisation (K2) · D14-loyal with a divergence log (K3) · no authenticity verdict — that is the human's click (K4) · address derived, no podium write (K5);
- the six lenses, including cold-restart (a fresh process rebuilds the same tree+ledger from disk alone) and blind-tool (unobservable reads INCONCLUSIVE).

**What is NOT being attested:** that a real descent has ever run on a live cell (no desk is constituted — B3
runs against fixture node trees, hold H-B3-1); nothing about B4–B6; and the letter-order is a **declared
parameter** (H-B3-2) carried for his later confirmation, not settled by this attestation.

## The drafted sentence — his to change, delete, or replace

> *"The descent — a gate that fails to lock becomes a child node with byte-exact axis inheritance, a guard
> pass at every depth, and a return of artifact + genuine ∞0′ — attest it."*

**He may say it differently, or say only "attest it". The machine never types his word for him.**

## His word — recorded exactly as he gave it

> **"attest it"**

*He was shown the drafted sentence and answered with his own short form. The machine did not type it for
him; the drafted sentence is left above unchanged so the record shows what he was offered and what he said.*

---

- `evidence.md` sha256: `6a462db62722da860284094d364bbca40013052bc72cbea5e4ac117b81f91933`
- `commission.md` sha256: `1c897c718e4edc6fc4381c9d4d752fe6f241e1a68ebf8f965b6427a444e693a6`
- authored files (sha256): `descent.py ccf33cbf…` · `surface_contract.py 46f9ce58…` · `selftest.py c45a2335…` · `phase-card.md 609fe088…`
- his box at the moment of attesting: `state/gates.jsonl` — 1 record, his plant (B3 is descent infrastructure, not a gate)
- date: **2026-08-29**
